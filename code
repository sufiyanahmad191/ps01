#include<iostream>
using namespace std;
int main(){
    char ch[100];
    cout<<"enter your code word : ";
    
        cin.get(ch,100);
        for(int i=0; i<100; i++){
            if (ch[i] == '\0') break;
            if((int)ch[i]==32) cout<<" ";
            cout << (char)((int)ch[i] - 3);
        }
    }
